{
  "name": "@grec0/mcp-oracle-db",
  "version": "0.1.4",
  "description": "Servidor MCP para integração completa com Oracle Database",
  "license": "MIT",
  "author": "@grec0",
  "homepage": "https://github.com/gcorroto/mcp-oracle-db",
  "bugs": "https://github.com/gcorroto/mcp-oracle-db/issues",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gcorroto/mcp-oracle-db.git"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "oracle",
    "database",
    "sql",
    "db",
    "oracledb"
  ],
  "type": "module",
  "main": "index.ts",
  "bin": {
    "mcp-oracle-db": "index.ts"
  },
  "files": [
    "dist/**/*",
    "README.md",
    "LICENSE"
  ],
  "publishConfig": {
    "registry": "https://registry.npmjs.org/",
    "access": "public"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "tsc && shx chmod +x dist/*.js",
    "watch": "tsc --watch",
    "start": "tsx index.ts",
    "start:http": "MCP_HTTP_HOST=127.0.0.1 MCP_HTTP_PORT=3100 tsx index.ts",
    "dev": "tsx index.ts",
    "inspector": "npx -y @modelcontextprotocol/inspector tsx index.ts",
    "test": "NODE_TLS_REJECT_UNAUTHORIZED=0 node --experimental-vm-modules node_modules/jest/bin/jest.js --testMatch='**/tests/**/*.test.ts'",
    "version:patch": "npm version patch --no-git-tag-version",
    "version:minor": "npm version minor --no-git-tag-version",
    "version:major": "npm version major --no-git-tag-version",
    "prerelease": "npm run build && npm test",
    "release:patch": "npm run version:patch && npm run prerelease && npm publish",
    "release:minor": "npm run version:minor && npm run prerelease && npm publish",
    "release:major": "npm run version:major && npm run prerelease && npm publish",
    "postpublish": "git add package.json && git commit -m \"chore: bump version to v$(node -p \"require('./package.json').version\")\" && git push"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "oracledb": "^6.10.0",
    "tsx": "^4.19.2",
    "zod": "^3.22.4",
    "zod-to-json-schema": "^3.23.5"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/jest": "^29.5.14",
    "@types/node": "^25.8.0",
    "jest": "^29.7.0",
    "shx": "^0.3.4",
    "ts-jest": "^29.2.6",
    "typescript": "^5.6.2"
  }
}
